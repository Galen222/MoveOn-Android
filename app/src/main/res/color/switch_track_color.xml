<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color cuando está activado (checked) -->
    <item android:color="@color/greenPrimary" android:state_checked="true"/>

    <!-- Color cuando está desactivado (unchecked) -->
    <item android:color="@color/disabled"/>
</selector>